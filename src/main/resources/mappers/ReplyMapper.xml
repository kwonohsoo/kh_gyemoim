<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gyemoim.dao.ReplyDAO">

  <select id="reply" resultType="com.gyemoim.domain.ReplyVO">
    SELECT *
    FROM REPLY
    WHERE bid = #{bid}
  </select>

</mapper>